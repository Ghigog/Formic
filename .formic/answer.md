{"title":"Add a To Do \"New request\" for tickets, with agent triage and attachments","prd":{"summary":"Today there is one way into the board: the Backlog's \"New request\" button, which always creates an Epic, and the Product Agent then drafts a PRD for it. This Epic adds a second \"New request\" button at the head of the To Do column. A request made there becomes a single ticket with no PRD. Each column's agent triages what lands in its column. The To Do (Architect) agent can send a request that is too big for one ticket back to Backlog as an Epic. The Backlog (Product) agent can send a request that is small enough to be one ticket over to To Do. Either way, the person is told what the agent did and why. Both New request dialogs will also accept image and file attachments, which go to the agent along with the text.","problem":"Every request becomes an Epic and goes through a full PRD and DAG breakdown, even a one-line fix that only needs one ticket. That is slow and costs money for small work. A person can't say \"this is just a ticket\". Once a request is filed, no agent checks whether it went to the right place. Requests also have to be text only, so a screenshot of a bug, a mockup or a spec file can't be shared with the agent. One thing the request leaves open: which products may be attached. This PRD scopes that to images plus common document and text files, with a per-file size limit. See outOfScope.","scope":["Add a \"New request\" button to the head of the To Do column that works like Backlog's NewRequestButton and opens the capture dialog in a ticket mode. Its title, placeholder and submit label say it makes a ticket, not a PRD.","A request made from To Do creates one standalone ticket in To Do that does not belong to any Epic. It has a key, a title taken from the request, and the raw request as its description. It gets no PRD and no DAG breakdown.","To Do triage: the agent assigned to the To Do column (Architect role, built-in or a ColumnAgent preset) reads each new To Do request. It either keeps it as a ticket, filling in a description, acceptance criteria, file scope and size, or decides it is Epic-sized. In that case it turns the request into an Epic in Backlog, and the Product Agent drafts a PRD for it as usual.","Backlog triage: the agent assigned to the Backlog column (Product role) reads each new Backlog request. It either drafts a PRD as it does today, or decides the request fits in one ticket. In that case it turns the request into a standalone ticket in To Do, without a PRD.","Tell the person the triage result: when an agent moves a request to the other column, the card shows the reason (for example, as a note on the card or in its drawer). The move also shows up live on the board through the existing SSE events.","Attachments in both New request dialogs: add files by using a file picker, dropping files onto the dialog, or pasting an image from the clipboard. Show the added files as a list with image thumbnails and a remove button for each. Reject a file with a clear error if its type is not allowed or it is too big.","Store attachments and link them to the Epic or ticket they came with. They stay linked when triage moves the request to the other column. Show them in the Epic drawer and the ticket drawer.","Send attachments to the triaging agent and to the agent that works the request next (Product Agent or Architect Agent): images as image input where the provider supports it, text-like files as text content, and anything else by name only.","Make the POST /api/epics body accept attachments, and add a matching create endpoint for To Do tickets that also accepts them."],"outOfScope":["Making the Backlog button stop creating Epics by default. It still creates an Epic and still runs the PRD step unless the agent sends the request to To Do.","Triage by the In Progress, In Review or Done column agents, and re-running triage on existing cards or on cards a person drags.","Asking the person to confirm before an agent moves a request. The agent moves it and then says why; the person can drag it back.","Attaching a standalone ticket to an existing Epic, or grouping several standalone tickets into a new Epic.","Adding, removing or editing attachments after the request is made.","Sending attachments to the Coder or Reviewer agents, or putting them in the sandbox or the PR.","Video or audio files, archives, attachments over the size limit, and scanning files for viruses.","Attachments in the Assistant chat.","Changing the eight lifecycle stages or the column set."],"technicalContext":["The capture dialog is src/components/board/new-item-dialog.tsx (NewItemDialog). It takes only rawRequest: string and always says \"Draft PRD\". board-shell.tsx connects it to createEpic. board.tsx renders NewRequestButton only when col === \"backlog\" (around line 395).","Creation goes through POST /api/epics (src/app/api/epics/route.ts, rawRequest 3–4000 chars) to createBacklogItem in src/lib/board/service.ts. That creates an Epic and launches runProductAgent. No code path creates a ticket without an Epic.","Tickets require an Epic today: ticketSchema.epicId in src/lib/domain/entities.ts and Ticket.epicId in prisma/schema.prisma are both required, and ticket keys are unique per [epicId, key]. A standalone ticket needs either a nullable epicId (with a new key scheme, plus the DAG, BoardCard and grouping code that assume a parent) or a hidden single-ticket Epic. This is the main design decision for the tickets. BoardCard.detached already exists for tickets pulled out of an Epic's group and may be reusable for display.","Column agents live in src/lib/agents/presets.ts (BUILD): product → backlog, architect → todo. They are resolved per project through ColumnAgent / agentConfigFor. runProductAgent and runArchitectAgent in src/lib/agents/pipeline.ts both have a CLI-agent path (cliAgentFor → startCliAnswer, answered by webhook) and an API path. Triage must work on both paths.","Today the Product Agent's output is only a PRD (prdSchema), and the Architect Agent's input is an Epic plus its PRD, which it breaks into a DAG. Both need a new output that also carries the triage decision and the reason. The Architect also needs a new mode for a single raw ticket request that has no PRD.","Status drives the column (src/lib/domain/status.ts). A standalone ticket starts in To Do as ready or waiting. \"specified\" is Epic-only. The existing misplacedIn / misplacedReason and blockedReason fields and the card.created event (src/lib/events/bus.ts) are the current ways to tell the person something on a card.","There is no file storage yet: the dependencies have no blob or S3 client, and the app deploys to Vercel (vercel.json), which has no persistent disk. Attachments need a storage backend plus an Attachment model in Prisma, with a matching implementation in memory-repository.ts for tests.","Model calls go through src/lib/agents/anthropic.ts, src/lib/agents/openai-agents.ts and src/lib/llm/openai-compat.ts. Image input support differs by provider and model (src/lib/llm/providers.ts), so there must be a text-only fallback."],"userStories":["As a board user, I'd like to add a request straight to To Do as a ticket, so that small, clear work skips the PRD and DAG steps.","As a board user, I'd like the To Do agent to tell me when my ticket is really Epic-sized and move it to Backlog, so that big work still gets a PRD.","As a board user, I'd like the Backlog agent to tell me when my request fits in one ticket and move it to To Do, so that I don't pay for a PRD it doesn't need.","As a board user, I'd like to attach screenshots, photos and files to a new request, so that the agent sees the bug, mockup or spec I'm describing.","As a board user, I'd like to see a request's attachments in its Epic or ticket drawer, so that I can check what the agent was given."],"successCriteria":["Given a board with a project, when the person looks at the To Do column, then it has a \"New request\" button at its head, and Backlog still has its own.","Given the To Do New request dialog, when the person submits a request that the To Do agent keeps, then one ticket appears in To Do that belongs to no Epic, and no Product Agent run starts for it.","Given a request made from To Do that the To Do agent judges Epic-sized, when triage finishes, then the card appears in Backlog as an Epic, the Product Agent drafts its PRD, and the card shows the agent's reason for the move.","Given a request made from Backlog that the Backlog agent judges ticket-sized, when triage finishes, then a standalone ticket appears in To Do with no PRD, and the card shows the agent's reason for the move.","Given a request made from Backlog that the Backlog agent keeps, when triage finishes, then the Epic gets a PRD exactly as it does today.","Given a triage move, when a second browser has the same board open, then it shows the card in its new column without a reload.","Given either New request dialog, when the person adds an image by picking it, dropping it or pasting it, then a thumbnail appears in the dialog and can be removed before submitting.","Given either New request dialog, when the person adds a file of a type that isn't allowed or that is over the size limit, then the dialog shows an error naming the file and does not submit it.","Given a request submitted with attachments, when the person opens its Epic or ticket drawer, then every attachment is listed and can be opened, including after a triage move.","Given a request submitted with an image, when the triaging agent runs on a model that supports image input, then the agent receives the image with the request text.","Given the column's agent fails during triage, when the run ends, then the card stays in the column where it was made, stalled, with the error shown as it is for other agent failures."]}}
